import 'package:flutter/material.dart';

class MemoriaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Memoria de cálculo'),
      ),
      body: Center(
        child: Text('Contenido de Memoria de cálculo'),
      ),
    );
  }
}
